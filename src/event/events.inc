#ifndef EVENT_SUBSYSTEM
    #define EVENT_SUBSYSTEM(subsystem_name)
#endif
#ifndef EVENT_NAME
    #define EVENT_NAME(subsystem_name, event_name)
#endif

EVENT_SUBSYSTEM(HART)
EVENT_NAME(HART, BEFORE_EXECUTE)
EVENT_NAME(HART, AFTER_EXECUTE)
EVENT_SUBSYSTEM(MEM)
EVENT_NAME(MEM, READ)
EVENT_NAME(MEM, WRITE)
EVENT_NAME(MEM, EXCEPTION)
EVENT_NAME(MEM, ALLOCATION)
EVENT_SUBSYSTEM(REG)
EVENT_NAME(REG, READ)
EVENT_NAME(REG, WRITE)

#undef EVENT_SUBSYSTEM
#undef EVENT_NAME
